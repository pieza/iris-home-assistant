{
  "config": {
    "flow_title": "{name}",
    "step": {
      "user": {
        "title": "IRIS",
        "description": "Connect to your IRIS bridge.",
        "data": {
          "host": "Host",
          "port": "Port",
          "name": "Name",
          "api_token": "API token"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not connect to IRIS. Check the host, port, and network connection.",
      "invalid_auth": "The API token is invalid. Run `iris home-assistant setup` and enter the token it prints."
    },
    "abort": {
      "already_configured": "This IRIS bridge is already configured."
    }
  },
  "services": {
    "send_command": {
      "name": "Send command",
      "description": "Send an IRIS remote command.",
      "fields": {
        "command": {
          "name": "Command",
          "description": "The command name from the device profile."
        }
      }
    }
  }
}
